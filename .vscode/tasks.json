{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build ppe (Debug)",
      "type": "shell",
      "command": "cmake --build --preset debug --target ppe",
      "problemMatcher": "$gcc"
    }
  ]
}
