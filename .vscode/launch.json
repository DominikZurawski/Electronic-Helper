{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ppe",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": ["list"],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "preLaunchTask": "Build ppe (Debug)"
    }
  ]
}
