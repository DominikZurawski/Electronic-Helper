{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ppe_gui",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "preLaunchTask": "Build ppe_gui (Debug)"
    }
  ]
}
